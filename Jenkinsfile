pipeline {
    agent any

    stages {
        stage('Build and Run Docker Container') {
            steps {
                script {
                    sh 'docker ps -a'
                }
            }
        }
    }
}
